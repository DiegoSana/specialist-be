# fly.toml app configuration file generated for specialist-api on 2025-12-15T17:09:15-03:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'specialist-api'
primary_region = 'gru'

[build]
  dockerfile = 'Dockerfile.prod'

[deploy]
  release_command = 'npx prisma migrate deploy'

[env]
  NODE_ENV = 'production'
  PORT = '8080'
  CORS_ORIGINS = 'http://localhost:3000,http://localhost:3001,http://127.0.0.1:3001,https://specialist-admin.vercel.app,https://specialist-fe.vercel.app'
  FRONTEND_URL = 'https://specialist-fe.vercel.app'
  NOTIFICATIONS_DISPATCH_ENABLED = 'true'
  NOTIFICATIONS_SMTP_PORT = '587'
  # Pre-launch: this "production" Fly deploy is only used for functional testing/validation
  # so far, not real users. WHATSAPP_PROVIDER=local + WHATSAPP_DEV_MODE_ENABLED=true turn on
  # the admin WhatsApp dev tools (simulate reply, force-trigger a follow-up) here too, same as
  # local dev - see docs/guides/whatsapp/README.md. Switch WHATSAPP_PROVIDER back to 'twilio'
  # and remove WHATSAPP_DEV_MODE_ENABLED once real WhatsApp/Twilio testing starts.
  WHATSAPP_PROVIDER = 'local'
  WHATSAPP_DEV_MODE_ENABLED = 'true'
  # Pre-launch: email delivery on this "production" Fly deploy is for functional testing only
  # (no real users yet). EMAIL_PROVIDER='ethereal' dynamically provisions a throwaway Ethereal
  # (ethereal.email) test-SMTP account at boot via nodemailer.createTestAccount() - zero secrets
  # to manage, nothing is really delivered, and each send's preview URL is logged and persisted
  # on the notification delivery (see GET /admin/notifications). Switch EMAIL_PROVIDER back to
  # 'smtp' or 'mailgun' with real credentials (as Fly secrets) once real email testing starts.
  EMAIL_PROVIDER = 'ethereal'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

  [[http_service.checks]]
    interval = '30s'
    timeout = '10s'
    grace_period = '30s'
    method = 'GET'
    path = '/api/health'
    protocol = 'http'

  [[http_service.checks]]
    interval = '1m0s'
    timeout = '10s'
    grace_period = '30s'
    method = 'GET'
    path = '/api/health/ready'
    protocol = 'http'

[[vm]]
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 512
